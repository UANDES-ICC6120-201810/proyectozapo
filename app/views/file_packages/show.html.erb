<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @file_package.name %>
</p>

<p>
  <strong>Files:</strong>
  <% @file_to_update.each do |file_update| %>
  <div class="col">
    <div class="checkbox">
      <% if @final_check.include? file_update.id %>
        <td><input type="checkbox" onclick="updateCheckBoxValue(event)" value="<%= file_update.id %>" class="btn btn-link " checked><%= file_update.name %></td>
      <% else %>
        <td><input type="checkbox" onclick="updateCheckBoxValue(event)" value="<%= file_update.id %>" class="btn btn-link"><%= file_update.name %></td>
      <% end %>
    </div>
  </div>
  <% end %>
</p>


<%= link_to 'Edit', edit_file_package_path(@file_package) %> |
<%= link_to 'Back', file_packages_path %>