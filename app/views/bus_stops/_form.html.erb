<div class="container">
  <div class="row vertical-offset-100">
    <div class="col-md-4 col-md-offset-4">
      <%= form_with(model: bus_stop, local: true) do |form| %>
        <% if bus_stop.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(bus_stop.errors.count, "error") %> prohibited this bus_stop from being saved:</h2>

            <ul>
              <% bus_stop.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="field">
          <%= form.label :code %>
          <%= form.text_field :code, :class => 'form-control' %>
        </div>

        <div class="field">
          <%= form.label :name %>
          <%= form.text_field :name, :class => 'form-control' %>
        </div>

        <div class="field">
          <%= form.label :stop_lat %>
          <%= form.text_field :stop_lat, :class => 'form-control' %>
        </div>

        <div class="field">
          <%= form.label :stop_lon %>
          <%= form.text_field :stop_lon, :class => 'form-control' %>
        </div>
        <ul></ul>
        <div class="actions">
          <%= form.submit "Create Bus Stop",:class => "btn btn-lg btn-info btn-block" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

